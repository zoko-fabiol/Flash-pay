{
  "name": "flash-pay-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint . --ext .ts",
    "start": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.4.1",
    "@sendgrid/mail": "^8.9.0",
    "twilio": "^4.13.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "eslint": "^8.40.0",
    "eslint-config-standard-with-typescript": "^34.1.0",
    "@types/node": "^20.3.1"
  }
}
